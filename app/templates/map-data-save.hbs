{{#if selectedTrail}}
    <button type="button" class="btn btn-default" {{action 'save' selectedTrail}}>
        <span class="glyphicon glyphicon-save" aria-hidden="true"></span>
    </button>
{{/if}}
