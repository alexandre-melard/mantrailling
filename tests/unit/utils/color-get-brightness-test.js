import colorGetBrightness from '../../../utils/color-get-brightness';
import { module, test } from 'qunit';

module('colorGetBrightness');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = colorGetBrightness();
  assert.ok(result);
});
