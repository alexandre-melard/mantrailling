import googleGeocoderAutocomplete from '../../../utils/google-geocoder-autocomplete';
import { module, test } from 'qunit';

module('googleGeocoderAutocomplete');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = googleGeocoderAutocomplete();
  assert.ok(result);
});
