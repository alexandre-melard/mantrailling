import mapFormatArea from '../../../utils/map-format-area';
import { module, test } from 'qunit';

module('mapFormatArea');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = mapFormatArea();
  assert.ok(result);
});
