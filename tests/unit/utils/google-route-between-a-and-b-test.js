import googleRouteBetweenAAndB from '../../../utils/google-route-between-a-and-b';
import { module, test } from 'qunit';

module('googleRouteBetweenAAndB');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = googleRouteBetweenAAndB();
  assert.ok(result);
});
