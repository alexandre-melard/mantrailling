import mapFormatLength from '../../../utils/map-format-length';
import { module, test } from 'qunit';

module('mapFormatLength');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = mapFormatLength();
  assert.ok(result);
});
