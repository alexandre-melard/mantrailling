import colorGetRgb from '../../../utils/color-get-rgb';
import { module, test } from 'qunit';

module('colorGetRgb');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = colorGetRgb();
  assert.ok(result);
});
