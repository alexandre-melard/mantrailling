import colorGetLuminance from '../../../utils/color-get-luminance';
import { module, test } from 'qunit';

module('colorGetLuminance');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = colorGetLuminance();
  assert.ok(result);
});
