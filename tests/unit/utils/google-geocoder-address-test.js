import googleGeocoderAddress from '../../../utils/google-geocoder-address';
import { module, test } from 'qunit';

module('googleGeocoderAddress');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = googleGeocoderAddress();
  assert.ok(result);
});
