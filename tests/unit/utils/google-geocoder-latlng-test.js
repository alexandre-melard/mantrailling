import googleGeocoderLatlng from '../../../utils/google-geocoder-latlng';
import { module, test } from 'qunit';

module('googleGeocoderLatlng');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = googleGeocoderLatlng();
  assert.ok(result);
});
